version: 2

seeds:
    - name: seed_product_price_tiers
      description: Price tier classification thresholds per product category.
      columns:
          - name: category
            tests:
                - not_null
          - name: min_price
            tests:
                - not_null
          - name: max_price
            tests:
                - not_null
          - name: tier_label
            tests:
                - not_null
