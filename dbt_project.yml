name: "marketing_orchestration"
version: "1.0.0"
config-version: 2
profile: "default"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_packages"

flags:
    static_analysis: false

vars:
    source_database: "{{ env_var('DBT_PROFILE_PROJECT', target.project) }}"
    ga4_database: "bigquery-testing-489807"
    ga4_schema: "analytics_526441677"
    supabase_schema: "dbt_supabase"
    hubspot_schema: "dbt_hubspot"
    meta_ads_schema: "analytics_526441677"
    linkedin_ads_schema: "analytics_526441677"
    salesforce_schema: "dbt_salesforce"

models:
    marketing_orchestration:
        staging:
            +materialized: view
        marts:
            +materialized: table
